{
  "name": "terraform-evidence-action",
  "version": "1.0.2",
  "private": true,
  "description": "Stream secret-minimised Terraform execution evidence to Chamber",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "npm run typecheck && ncc build src/index.ts -o dist --minify --source-map --license licenses.txt",
    "bundle:check": "npm run build && git diff --exit-code -- dist",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "tsx --test test/**/*.test.ts",
    "test:coverage": "tsx --test --experimental-test-coverage test/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "3.0.1"
  },
  "devDependencies": {
    "@eslint/js": "10.0.0",
    "@types/node": "26.0.0",
    "@vercel/ncc": "0.44.1",
    "eslint": "10.0.0",
    "prettier": "3.6.2",
    "tsx": "4.23.12",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0"
  }
}
